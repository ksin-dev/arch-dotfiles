# Monitor rules are intentionally machine-local.
# Use SUPER+SHIFT+P to open nwg-displays and save the real layout to
# ~/.config/dotfiles/hypr/monitors.conf.
# Keep this generic fallback so Hyprland has a usable output before setup.

monitor=,preferred,auto,1
