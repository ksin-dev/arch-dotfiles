# Herdr

Herdr reads `~/.config/herdr/config.toml`.

This dotfiles repo links only that file instead of the whole `~/.config/herdr`
directory, because Herdr also writes runtime logs, sockets, and session state
next to the config file.

The configured prefix is `ctrl+alt`.
